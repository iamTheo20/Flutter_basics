
import 'package:flutter/material.dart';

class MeasurePage extends StatelessWidget {
  @override
  Widget build(BuildContext context) => Scaffold(
    appBar: AppBar(
      backgroundColor: Color.fromRGBO(41, 50, 55, 1.0),
      title: Text('Measure'),

    ),
    body: Center(child: Text('Measure')),
    backgroundColor: Color.fromRGBO(41, 50, 55, 1.0),
  ); 
}